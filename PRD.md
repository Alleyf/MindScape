## 项目名称
**MindScape**

## 核心定位
> “一个会呼吸、会想象、会连接的个人思维宇宙 —— 不止是记录，更是与知识的偶遇。”

## 目标用户
- 喜欢用 Markdown 写笔记、想法、碎片灵感的人
- 讨厌传统 wiki 的枯燥列表感
- 希望知识库有“艺术展”般浏览体验的人

---

## 1. 核心功能需求（简版）

| 模块 | 功能 | 说明 |
|------|------|------|
| 内容编辑 | 支持 Markdown + 元数据 | 标准 md，支持 frontmatter（标题、标签、日期、情绪图标等） |
| 内容存储 | 本地文件 / Git 仓库 | 纯文本优先，不锁数据 |
| 页面渲染 | 动态 / 静态混合 | 静态为主，动态 AI 组件按需加载 |
| 双向链接 | 自动识别 `[[xxx]]` | 展示反向链接图谱 |
| AI 能力 | 智能链接、自动摘要、创意标题联想、语义漫游 | 核心差异化点 |
| 个性化 | 主题 / 布局 / 视觉“氛围”可调 | 支持暗黑 + 渐变 + 动态背景 |

---

## 2. 视觉与交互创意要求（惊艳之处）

> ⚡ 以下为“出其不意”的关键，建议重点实现其中 3–4 项

### 首页 / 探索视图
- **非网格 / 非列表布局**：采用 **知识星云 / 气泡图 / 有机浮点**
- 每篇文档是一个“悬浮卡片或粒子”
- 鼠标悬浮 → 文档片段扩散放大
- 支持 **“随机漫步”按钮**：一键跳转到一篇你几乎忘了的旧笔记
- 背景为 **动态渐变 + 微弱粒子流**（随内容主题微变）

### 文章页视觉
- 第一屏不直接显示标题，而是显示一个 **“生成的隐喻图像或句子”**（例如：“这篇文章像 — 深夜电台里的一把木吉他”）
- 正文阅读时，右侧自动生成 **“AI 共振栏”**：
  - 读这段时想到的其他笔记片段
  - 一段情绪隐喻短文
  - 一个无关但有趣的类比

### 交互动效
- 滚动时内容与背景产生 **视差 + 极轻微位移模糊**
- 点击链接时 **不是新页面，而是“原地溶解再凝合”**
- 暗色模式下，鼠标周围带微弱光晕（像手电筒照知识）

---

## 3. AI-Native 智能层（核心惊艳点）

| AI 能力 | 触发方式 | 效果 |
|--------|----------|------|
| 语义漫游 | 每次打开随机或点击“漫游” | AI 串联 3 篇语义相近但不直接相关的笔记，生成一条“思维小径” |
| 自动灵魂标题 | 可选 | 给每篇笔记生成 1 个浪漫/隐喻式的副标题 |
| 内容联想卡片 | 文章底部 | “假如你在写这篇文章时，还想过这些……” |
| AI 总结海报 | 可选 | 一键生成可分享的图文摘要（像展览海报） |
| 笔记人格化 | 可选实验功能 | 每篇笔记会生成一个“拟人标签”：理性、躁动、温柔、谜语人 |

---

## 4. 信息结构与内容模型

```yaml
每篇文档结构：
- 标题
- 正文（md）
- 标签数组
- 情绪/能量标签（可选）
- 创建时间 / 最后修改
- AI 生成的关联笔记列表（存于元数据或动态计算）
```

> 不强制复杂数据库，推荐使用 **静态生成 + 运行时向量检索** 的方式实现 AI 智能感。

---

## 5. 技术实现建议（简单 + 可控）

| 层级 | 推荐方案 | 理由 |
|------|----------|------|
| 框架 | Astro / Next.js (SSG) | 兼顾性能与动态组件 |
| 样式 | Tailwind + Framer Motion | 创意动效不痛苦 |
| MD 处理 | Markdoc / remark | 灵活扩展 |
| AI 服务 | 本地 embedding + OpenAI / 混元 / 智谱 API | 语义搜索与联想 |
| 存储 | 本地文件夹 + Git | 纯文本，你的数据永远是你的 |
| 部署 | Vercel / Cloudflare Pages | 免费且快 |

---

## 6. 非功能需求

- 打开任意页面 **首屏 < 0.8s**（静态优先）
- 完全支持 **dark mode**
- 移动端至少保证“可读”，不追求全动画效果
- 支持导出单篇笔记为 **图片 / PDF / 纯文本**

---

## 7. 一个可以用作 AI 生成初版界面的 Prompt 示例

> 你可以把下面这段直接复制给 AI 绘图或前端生成工具（如 v0 / Galileo AI）：

```
你是一个极富创意的前端与 AI 产品设计师。

请为一个名为「NebulaWiki」的 AI-Native 个人知识库/wikiblog 设计主界面。

核心特点：
- 不是传统列表或网格
- 首页以「动态知识星云」或「有机粒子场」的形式展示所有笔记卡片
- 每篇笔记是一个悬浮半透明卡片，内容为标题 + AI 生成的一句隐喻式描述
- 有一个「随机漫步」的发光按钮
- 背景为深色 + 缓慢流动的渐变粒子
- 阅读文章时，右侧有一个「AI Resonance」侧栏，动态展示当前段落相关的其他笔记片段
- 整体风格：科幻、温和、诗意，而非炫技和冰冷

输出 HTML/CSS/JS 代码（含基础 React 或纯 HTML 均可），请加入平滑的悬停与滚动动效。
```

---

## 8. 落地建议

- **不要一上来做全部 AI 功能**：先实现“美观 + 随机漫步 + md 渲染”
- 第一周让自己用起来 → 第二周加入语义联想 → 第三周加入人格化生成
- **真正的惊艳 = 稳定好用 + 1–2 个真的意想不到的细节**
